pipeline {
    agent any

    stages {
        stage('test') {
            steps {
              dir('ViQuJob') {
                bat 'mvn clean compile test'
              }
            }
        }
    }
}
